---
globs: src/components/**,src/store/**,src/index.css
alwaysApply: false
---

Read `.cursor/skills/ui-components/SKILL.md` before adding or editing a component.

- HeroUI v3 is built on React Aria: `onPress` and `isDisabled`, not `onClick` and `disabled`. Import
  from the component's own subpath, as in `@heroui/react/button`.
- Use `Chip` for a read-only pill. HeroUI's `Tag` is a collection item and throws outside a
  `TagGroup`, which is not a reason to hand-roll one.
- Style with the semantic tokens — `bg-surface`, `text-muted`, `border-border` — rather than raw
  palette colours, so both themes keep working. Dark is reachable now; look at it.
- Every field needs a name a test can query: a `Label` inside a `TextField`, or an `aria-label` where
  there is no room for one. Inside a `TextField`, reach for `Description` and `FieldError` rather
  than wiring `aria-describedby` by hand.
- Select narrowly from the store in anything on the streaming path; generation patches it on every
  token.
- Assistant text renders through `ui/RichText.tsx`, which builds elements and never HTML. Nothing in
  a reply may become markup by any other route. Reasoning is the exception and goes through
  `ui/Reasoning.tsx` as plain text, so a URL the model invented never becomes a link.
- `Spinner` carries its own `aria-label="Loading"`. Inside a button that is part of the accessible
  name, so pass `aria-hidden` when the row already says what is happening.
